# Optional when exactly one authorized device is connected.
# PHONE_SERIAL=your-adb-serial
# PHONE_GATEWAY_AUDIT=/home/you/.local/state/ctlphone/gateway-audit.jsonl

# Keep real serials, paths, unlock data, credentials, and tokens only in your
# local environment. Never commit them to this repository.
